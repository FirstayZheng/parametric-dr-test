#ATTENTION: NO TAB. Please use SPACE and be careful about the hierarchy.
#TODO Add hierarchy
prefix: 'swissroll'
dataset: 'swissroll'
dataset_category: 'local' # the dataset store locally and use cache-files when computing knn if `local`, else generate real-time
batch_size: 128
n_neighbors: 12
device: 0
color: 3
lr: 1.0e-3
global_loss: 1.0e-6
draw_gap: 1
epochs: 1
batch_gap: 100
seed: 42
output_dir: '/home/zengxingchen/code/parametric-dr/outputs'
negative_sample_rate: 5
loss_mode: 'noGlobalLoss'
save_procedure_data: False